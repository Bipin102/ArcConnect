NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id_here

# testnet | mainnet — see lib/networks.ts. Mainnet is a placeholder until Arc
# mainnet actually ships; setting this to "mainnet" today has no effect and
# the app silently stays on testnet.
NEXT_PUBLIC_ARC_NETWORK=testnet
